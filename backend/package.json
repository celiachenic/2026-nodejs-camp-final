{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ./bin/www.js",
    "start": "node ./bin/www.js",
    "migration:generate": "typeorm migration:generate -d ./db/dataSource.js -o",
    "migration:show": "typeorm migration:show -d ./db/dataSource.js",
    "migration:run": "typeorm migration:run -d ./db/dataSource.js",
    "migration:revert": "typeorm migration:revert -d ./db/dataSource.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.23.0",
    "typeorm": "^1.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
